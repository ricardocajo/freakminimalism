// This file is no longer needed since all cart functionality is handled client-side through CartContext
export type Product = {
  _id: string;
  name: string;
  price: number;
  quantity: number;
};
